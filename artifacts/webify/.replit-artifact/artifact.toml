kind = "web"
previewPath = "/"
title = "Webify"
version = "1.0.0"
id = "artifacts/webify"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22506

[services.development]
run = "pnpm --filter @workspace/webify run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/webify", "run", "build" ]
publicDir = "artifacts/webify/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22506"
BASE_PATH = "/"
